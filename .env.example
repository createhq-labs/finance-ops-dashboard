NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google OAuth is configured in the Supabase Dashboard (Auth > Providers), not here.
# Keep this false until that provider setup is complete.
NEXT_PUBLIC_GOOGLE_LOGIN_ENABLED=false